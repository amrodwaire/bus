import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.yourname.busconnect',
    appName: 'كوستر',
    webDir: 'dist/public'
};

export default config;